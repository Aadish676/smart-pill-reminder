<!DOCTYPE html>
<html>
<head><title>Dashboard</title></head>
<body>
<h2>Welcome, {{ current_user.username }}</h2>
<h3>Family Members</h3>
<ul>
{% for member in members %}
  <li>
    {{ member.name }} ({{ member.phone }}) - Notify via {{ member.notify_method }}
    <form method="POST" action="/add_pill/{{ member.id }}">
      Pill Name: <input name="pill_name"> Time (HH:MM): <input name="pill_time">
      <input type="submit" value="Add Pill">
    </form>
    <ul>
      {% for pill in member.pills %}
        <li>{{ pill.name }} at {{ pill.time }} - {{ pill.status }}</li>
      {% endfor %}
    </ul>
  </li>
{% endfor %}
</ul>

<h3>Add Family Member</h3>
<form method="POST" action="/add_member">
  Name: <input name="name"> Phone: <input name="phone">
  Notify via:
  <select name="notify">
    <option value="email">Email</option>
    <option value="sms">SMS</option>
    <option value="whatsapp">WhatsApp</option>
  </select>
  <input type="submit" value="Add">
</form>

<a href="/logout">Logout</a>
</body>
</html>
